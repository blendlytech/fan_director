.superdesign
.claude/scheduled_tasks.lock

# Local secrets: never commit (doc 11 §3 rule 7)
.dev.vars
.dev.vars.*
# Phase 3 live AI runs: the spend baseline stays on this machine.
worker/.live-spend.json
# Local-only seeds: they name accounts on one machine's staging, not the product.
worker/seeds/local-*.sql
.env
.env.*
.superdesign/tmp/
clerk-cli/

# Playwright MCP snapshots and console logs
.playwright-mcp/

# Local agent tooling, not part of the app
.agents/
skills-lock.json
mcp-gemini-server/

# Throwaway builds used for comparisons
frontend/dist-*/
